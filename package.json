{
  "name": "relis-foundry",
  "version": "0.2.4",
  "private": true,
  "type": "module",
  "description": "Système Foundry VTT RE:LIS — RE: Lost in Space",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests tools vite.config.ts eslint.config.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test:unit": "vitest run",
    "validate:package": "node tools/validate-package.mjs",
    "test": "npm run typecheck && npm run lint && npm run format:check && npm run test:unit && npm run build && npm run validate:package",
    "package": "npm run test && node tools/package-release.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.10.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.70.0",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  }
}
