{
  "name": "relis-foundry",
  "version": "0.8.2",
  "private": true,
  "type": "module",
  "description": "Système Foundry VTT RE:LIS — RE: Lost in Space",
  "scripts": {
    "build": "vite build",
    "content:validate": "node tools/personal-content.mjs validate",
    "content:generate": "node tools/personal-content.mjs generate",
    "content:build": "node tools/personal-content.mjs build",
    "content:check": "node tools/personal-content.mjs check",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests tools vite.config.ts eslint.config.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test:unit": "vitest run",
    "validate:package": "node tools/validate-package.mjs",
    "test": "npm run content:check && npm run typecheck && npm run lint && npm run format:check && npm run test:unit && npm run build && npm run validate:package",
    "package": "npm run test && npm run content:build && node tools/package-release.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "3.0.4",
    "@eslint/js": "10.0.1",
    "eslint": "10.10.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.70.0",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  }
}
